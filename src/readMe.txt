How to run application :
- mvn clean install -P dockerize
and after success(in terminal) :
- docker-compose -f ./docker-compose.yaml up -d

What you must install before? :
-Docker
-Java17

Remaining files will be installed automatically.
Unfortunately, I didn't have time to write the tests and make swagger work...
In mail there is a postman collection. You can use it!
Have a nice day!